extends base.pug
include mixins/video

block content
    form(method="GET")
        input(placeholder="Search by title",name="keyword", type="text")
        input(placeholder="Search now", type="submit")

    each video in videos 
        +video(video)